import React from 'react';
import CompilerServiceResult from '@/types/CompilerServiceResult';

interface RunCodeResultsProps {
    runResults: CompilerServiceResult;
}

function RunCodeResults({runResults}: RunCodeResultsProps) {
    return (
        <>
        </>
    )
};

export default RunCodeResults;
